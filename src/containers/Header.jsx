import * as React from 'react'
import Navbar from '@components/Navbar'

const Header = () => {
    return (
        <header>
            <img src="#" alt="" />
            <Navbar />
        </header>
    )
}

export default Header
